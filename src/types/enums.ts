export enum Team {
    Blue,
    Red,
}

export enum WordType {
    Blue,
    Red,
    Yellow,
    Black
}

export enum GameAction {
    Start,
    End,
    JoinBlue,
    JoinRed,
    Quit,
    Leader
}